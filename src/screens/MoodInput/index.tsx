import React from 'react';
import { View, Text } from 'react-native';

const MoodInputScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Mood Input Screen</Text>
    </View>
  );
};

export default MoodInputScreen;
